# Aswathy Vaikkttil Vinod

# Install and Import the vcd package
install.packages("vcd")
library("vcd")

# Plot a sales ~ temp scatter plot using the data below
Sales_data <- c(7,11,15,20,19,11,18,10,6,22)
Temperature_data <- c(69,81,77,84,80,97,87,70,65,90)
Sales_data
Temperature_data
plot(Sales_data, Temperature_data, main = "Scatter plot indicating the Sales and Temperature data")

# The mean of temperature data
mean(Temperature_data)

# Delete the 3rd element from the sales vector
Sales_data <- Sales_data[-3]
Sales_data

# Insert 16 as the 3rd element into the sales vector
Sales_data <- c(c(7,11),16,c(20,19,11,18,10,6,22))
Sales_data

# Create a vector <names> with elements Tom, Dick, Harry
Names <- c("Tom", "Dick", "Harry")
Names

# Create a 5 row and 2 column matrix of 10 integers
Numbers <- matrix(1:10, nrow = 5, ncol = 2)
Numbers

# Create a data frame <icSales> with sales and temp attributes
icSales <- data.frame(Sales_data, Temperature_data)
icSales

# Display the data frame structure of icScales
str(icSales)

# Display a summary of the icScales data frame
summary(icSales)

# Import the dataset Student.csv
getwd()
Student <- read.csv("//Users//aswathyvinod//Desktop//R//Student.csv", header = TRUE, sep = ",")
Student

# Display Variables in Student dataset
names(Student)

